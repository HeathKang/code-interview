# Knowledge Notes

Keep each note short enough to review in under two minutes.

- `network/`: protocols, transport, DNS, HTTP, caching, load balancing
- `database/`: indexing, transactions, isolation, replication, storage engines
- `os/`: processes, threads, memory, scheduling, filesystems, synchronization

Preferred note format:

```md
# Topic Name

- What it is:
- Why it matters:
- Interview answer:
- Common confusion:
```
